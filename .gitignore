# secrets & credentials
.env
.env.*
!.env.example

# node (no trailing slash: also ignore the symlinked node_modules the plugin
# SessionStart hook creates, pointing at CLAUDE_PLUGIN_DATA)
node_modules

# runtime data (sessions, recon artifacts, screenshots, dedup ledger, scratch)
.agentdocs/asm/sessions/
.agentdocs/asm/state/
.agentdocs/asm/recon/
.agentdocs/asm/artifacts/
.agentdocs/asm/tmp/

# log
.agentdocs/logs/

# OS
.DS_Store
